language: node_js
node_js:
  - "12"

cache:
  npm: false
  directories:
    - "~/.pnpm-store"

before_install:
  - npm install -g pnpm
  - pnpm config set store-dir ~/.pnpm-store

install:
  - pnpm install --frozen-lockfile

script:
  - pnpm run lint
  - pnpm run build
  - pnpm test
